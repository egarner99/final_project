## Final Submission Notes: 

In regards to re-running the project, the data files for the project (GM317 files, reference genome & gtf file) found in the  `data/` dir can be used OR it can also be redownloaded if needed following the steps in the protocol. I wasn't too sure if instructions regarding creating the `scripts/` dir or the README files needed to be added to the protocol (as they would already exist), so I left those alone. The `downloading_files/`, `fastqc/`, `star/`, and `trimgalore/` dirs can be ignored in the git repository, they are remnants from before the dirs were reorganized. For the R Studio portion, I attempted to make each of the main graphs mentioned in the Week14a section of the class website on the Quatro document (PCA plot, Volcano Plot, Plot for specific genes), following the steps and code mentioned there; I put them in a dir called `r_studio/`, since it technically isn't a script. At the end of the `README_notebook.md` file, I attached some of the references I used during the presentation and while working on the project. Some of them were a bit difficult to determine how to reference, as they were more similar to forum threads, so some information may be missing. Finally, if I used GitHub Copilot, I tried to make sure I mentioned how in the `README_notebook.md` file. I did save a document of all the questions I asked and the output; I wasn't sure if that was something I should add to the `final_project/` dir, but I would be happy to share it if needed! 

